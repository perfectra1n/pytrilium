# DEPRECATED: This workflow has been replaced by release.yml and test.yml
# It is kept for historical purposes but will not run
# Please use the new workflows for releases and testing

name: "[DEPRECATED] Format code, create binaries, publish to Pypi"

on:
  workflow_dispatch:  # Manual trigger only, no automatic runs

jobs:
  deprecated-notice:
    runs-on: ubuntu-latest
    steps:
      - name: Deprecated workflow notice  
        run: |
          echo "⚠️  This workflow has been deprecated"
          echo "📄 Please use .github/workflows/release.yml for releases"
          echo "🧪 Please use .github/workflows/test.yml for testing"
          echo "🚀 New workflows provide better testing, version checking, and release management"